# AGENTS

## Workflow
- After any code change: run `cargo fmt`, then `cargo clippy`, then `cargo build` (debug only).
- Do not build release artifacts; release builds are manual by the user.

## Notes
- Project is a Rust CLI named `call-mcp`.
